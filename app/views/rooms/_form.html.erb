<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :roomNumber %>
    <%= form.number_field :roomNumber, id: :room_roomNumber %>
  </div>

  <div class="field">
    <%= form.label :hotelId %>
    <%= form.number_field :hotelId, id: :room_hotelId %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :room_category %>
  </div>

  <div class="field">
    <%= form.label :maxOccupancy %>
    <%= form.number_field :maxOccupancy, id: :room_maxOccupancy %>
  </div>

  <div class="field">
    <%= form.label :availability %>
    <%= form.check_box :availability, id: :room_availability %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
