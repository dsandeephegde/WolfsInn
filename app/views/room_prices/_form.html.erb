<%= form_with(model: room_price, local: true) do |form| %>
  <% if room_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_price.errors.count, "error") %> prohibited this room_price from being saved:</h2>

      <ul>
      <% room_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :room_price_category %>
  </div>

  <div class="field">
    <%= form.label :maxOccupancy %>
    <%= form.number_field :maxOccupancy, id: :room_price_maxOccupancy %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :room_price_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
